/*made by: colin duffycreated: nov.11.2008*/trace = fl.trace;var select = fl.getDocumentDOM ().getTimeline ().getSelectedLayers ();settings = fl.getDocumentDOM ().xmlPanel (fl.configURI + "Commands/Timeline - Layer Settings.xml");if (settings.dismiss == "accept") {		var totalLayers = fl.getDocumentDOM ().getTimeline ().getSelectedLayers ();		var layer = fl.getDocumentDOM ().getTimeline ();		for (i = 0; i < totalLayers.length; i ++) {				// visible				(settings._seen == 'true') ? layer.layers[totalLayers[i]].visible = true : layer.layers[totalLayers[i]].visible = false;				// locked				(settings._locked == 'true') ? layer.layers[totalLayers[i]].locked = true : layer.layers[totalLayers[i]].locked = false;				// layer type				if (settings._type == '_normal') {			layer.layers[totalLayers[i]].layerType = 'normal';		} else if (settings._type == '_guide') {			layer.layers[totalLayers[i]].layerType = 'guide';		} else if (settings._type == '_mask') {			layer.layers[totalLayers[i]].layerType = 'mask';		} else if (settings._type == '_folder') {			layer.layers[totalLayers[i]].layerType = 'folder';		}				if (settings._color.length == 6) {			layer.layers[totalLayers[i]].color = '#' +  settings._color;		}	}}